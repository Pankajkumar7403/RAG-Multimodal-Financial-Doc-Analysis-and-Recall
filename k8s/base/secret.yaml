# NEVER commit real values — use Sealed Secrets, ESO, or Vault in production
# This file shows the structure only; populate via CI/CD pipeline
apiVersion: v1
kind: Secret
metadata:
  name: rag-financial-secrets
  labels:
    app: rag-financial
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_SECRET_MANAGER"
  ACTIVELOOP_TOKEN: ""
  RAG_API_MASTER_KEY: "REPLACE_WITH_GENERATED_KEY"
  COHERE_API_KEY: ""
