# Slim deps for Railway / HF Spaces (CPU).
# Torch is installed separately in the Dockerfile from the CPU wheel index.
streamlit>=1.35.0
watchdog>=4.0.0
python-dotenv>=1.0.0

# Core RAG runtime
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
structlog>=24.1.0
prometheus-client>=0.20.0
pyyaml>=6.0.1
psutil>=5.9.0
tqdm>=4.66.0
numpy>=1.26.0
Pillow>=10.3.0
pypdf>=4.0.0

# Local embeddings (BAAI/bge-small-en-v1.5)
sentence-transformers>=3.0.0

# Optional cache client (disabled at runtime; keep import-safe)
redis>=5.0.0
